# Required
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Database — SQLite for local dev (default), PostgreSQL for production
# Local:      sqlite+aiosqlite:///./clearpath.db  (default, no need to set)
# Production: postgresql+asyncpg://user:password@host:5432/dbname
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# Production frontend URL (set on Render so CORS allows your Vercel domain)
FRONTEND_URL=https://your-app.vercel.app

# State adapter API keys
# California SOS — register free at https://calicodev.sos.ca.gov/
CA_SOS_API_KEY=
